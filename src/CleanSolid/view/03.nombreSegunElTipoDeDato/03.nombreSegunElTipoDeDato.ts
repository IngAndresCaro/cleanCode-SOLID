() => {
  //Ejercicios
  //arreglo de temperaturas celsius
  const arrayOfNums = [33.6, 12.24];

  //dirección ip del servido
  const ip = "123.123.123.123";

  //Listado de usuarios
  const people = [
    { id: 1, email: "fermamdo@google.com" },
    { id: 2, email: "juan@google.com" },
  ];

  //Listado de emails de los usuarios
  const emails = people.map((u) => u.email);

  //variables booleanas de un video juego
  const jump = false;
  const run = true;
  const moTieneITems = true;
  const loading = true;

  //Solución
  //1
  const tempeturesOfCelsius = [33.6, 12.24];

  //2
  const severIp = "123.123.123.123";

  //3
  const users = [
    { id: 1, email: "fermamdo@google.com" },
    { id: 2, email: "juan@google.com" },
  ];

  //4
  const userEmails = users.map((user) => user.email);

  //5
  const isJumpPLayer = false;
  const canRunPLayer = true;
  const hasITems = true;
  const isloading = true;
};
